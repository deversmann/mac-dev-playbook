# code: language=ansible
---

- name: Set up finder defaults
  community.general.osx_defaults:
    domain: {{ item.domain }}
    key: {{ item.key }}
    type: {{ item.type }}
    value: {{ item.value }}
    state: present
  loop: finder_defaults

...
